@{
    ViewData["Title"] = "Home Page";
}

@*<div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    </div>*@



@*@model ContentstackRazorPagesExample.Pages.Products.IndexModel*@

@model List<Ecommerce.Entity.Product>

<div class="row">
    @if (Model.Any())
    {
        @foreach (var product in Model)
        {
            <div class="col-xs-12 col-sm-6 col-md-4">
                <a  href="/Home/NewOrder/@product.ProductId">
                    <div class="product-grids product-item">
                        <div class="product-img">
                            <img class="image-product" src="@product.Url" alt=""  style="width:100%;"/>
                        </div>

                        <h4 style="margin-left:100px;">@product.Title</h4>
                        <h5 style="margin-left:100px;">$@product.Price</h5>
                        <input style="margin-left:100px;" type="submit" value="Sipariş Ver"/>
                    </div>
                </a>
            </div>
        }

        @*if (Model.TotalPages > 1)
            {
                <div class="col-12">
                    @if (Model.ShowPrevious)
                    {
                        <a asp-page="./Index" asp-route-CurrentPage="@(Model.CurrentPage - 1)" class="btn btn-primary">Previous</a>
                    }
                    @if (Model.ShowNext)
                    {
                        <a asp-page="./Index" asp-route-CurrentPage="@(Model.CurrentPage + 1)" class="btn btn-primary">Next</a>
                    }
                </div>
            }*@
    }
    else
    {
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    You have not created or published any Product entries.
                </div>
            </div>
        </div>
    }
</div>